{
  "name": "uptime-robot",
  "version": "1.0.0",
  "description": "Self-hosted uptime monitor — keeps running even when the browser is closed",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
